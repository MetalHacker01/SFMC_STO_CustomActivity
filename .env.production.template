# Production Environment Variables Template
# Copy this to your deployment platform's environment variables section

# Application Configuration
NODE_ENV=production
PORT=10000

# JWT Configuration (REQUIRED - Generate secure values)
JWT_SECRET=REPLACE_WITH_SECURE_RANDOM_STRING_AT_LEAST_32_CHARS
APP_EXTENSION_KEY=sto-activity-production-key

# SFMC Configuration (REQUIRED - Replace with your actual SFMC credentials)
SFMC_CLIENT_ID=your-actual-sfmc-client-id
SFMC_CLIENT_SECRET=your-actual-sfmc-client-secret
SFMC_SUBDOMAIN=your-actual-sfmc-subdomain
SFMC_ACCOUNT_ID=your-actual-sfmc-account-id
SFMC_AUTH_URL=https://auth.exacttargetapis.com/v1/requestToken
SFMC_REST_BASE_URL=https://your-subdomain.rest.marketingcloudapis.com

# STO Configuration
STO_DEFAULT_TIMEZONE=America/Chicago
STO_HOLIDAY_API_URL=https://date.nager.at/api/v3
STO_HOLIDAY_API_ENABLED=true
STO_CACHE_TIMEOUT=3600
STO_MAX_RETRIES=3
STO_RETRY_DELAY=1000

# Logging Configuration
LOG_LEVEL=info
LOG_FILE_ENABLED=false
LOG_CONSOLE_ENABLED=true

# Security Configuration
CORS_ENABLED=true
RATE_LIMIT_ENABLED=true
HELMET_ENABLED=true

# Development Features (disable in production)
ENABLE_DEBUG_ENDPOINTS=false
ENABLE_MOCK_SFMC_API=false

# Instructions:
# 1. Replace all "your-actual-*" values with real SFMC credentials
# 2. Generate a secure JWT_SECRET (use: openssl rand -base64 32)
# 3. Set appropriate timezone for your use case
# 4. Disable debug endpoints for production security